 <h1>New Workout</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for Workouts:</p>
     <ul>
       <li></li>
       <li>Never insult dogs.</li>
       <li>Smile when you type.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for [current_user, @workout] do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter Workout Title" %>
       </div>
       <div class="form-group">
         <%= f.label :muscle_group %>
         <%= f.text_field :muscle_group, class: 'form-control', placeholder: "Enter Targeted Muscle Group" %>
       </div>
       <div class="form-group">
         <%= f.label :day %>
         <%= f.date_select :day, class: 'form-control', placeholder: "Enter Date To Be Performed." %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 20, class: 'form-control', placeholder: "This is where you would enter your workout. Example Format: Lift1| Rep Scheme|  Weight(lbs.)" %>
       </div>
       <div class="form-group">
         <%= f.label :notes %>
         <%= f.text_area :notes, rows: 5, class: 'form-control', placeholder: "This space is to provide extra information/TIPS/Words of Wisdom." %>
       </div>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>
